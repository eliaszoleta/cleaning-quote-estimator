{
  "buildCommand": "CI=false npm run build",
  "outputDirectory": "build",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://cleaning-quote-estimator-production.up.railway.app/api/:path*"
    }
  ]
}
